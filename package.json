{
  "name": "verifier-branded-page",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build": "node build.js",
    "clean": "rm -rf dist",
    "start:dist": "node index.js",
    "deploy": "npm run build && npm run deploy:local",
    "deploy:local": "gh-pages -d dist -b gh-pages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tensorflow/tfjs-node": "^4.17.0",
    "canvas": "^2.11.2",
    "dotenv": "17.4.2",
    "express": "^4.18.2",
    "face-api.js": "^0.22.2"
  },
  "devDependencies": {
    "gh-pages": "4.0.0"
  }
}
